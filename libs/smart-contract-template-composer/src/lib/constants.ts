export enum DownloadFrequencyType {
    Hour = 'hour',
    Day = 'day',
    Week = 'week',
    Month = 'month',
    Year = 'year',
}

export enum SubscriptionFrequencyType {
    Monthly = 'month',
    Annual = 'year',
}

export enum MonetizationMethod {
    One_Off = 'one-off',
    Subscription = 'subscription',
    Nft = 'nft',
    Investment = 'investment plan',
}
